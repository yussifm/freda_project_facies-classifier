services:
  - type: web
    name: facies-classifier
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: streamlit run facies_app.py --server.port 10000 --server.address 0.0.0.0
    plan: free
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0